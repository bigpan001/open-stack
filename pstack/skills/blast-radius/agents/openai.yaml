interface:
  display_name: "Blast Radius"
  short_description: "Run the Blast Radius pstack workflow in Codex."
  default_prompt: "Use $blast-radius for this request."
policy:
  allow_implicit_invocation: false
